services:
      ican_srapper:
            build: .
            container_name: ican_scrapper
            restart: unless-stopped

            env_file:
                  - .env 

            volumes:
                  - ./data:/app/data