services:
      ican_srapper:
            build: .
            container_name: ican_scrapper
            restart: unless-stopped

            environment:
                  - TZ=Europe/Moscow

            env_file:
                  - .env 

            volumes:
                  - ./data:/app/data